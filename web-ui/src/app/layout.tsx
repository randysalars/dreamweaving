import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title:
    "Dreamweaving Traditions, Archetypes, and Guided Journeys | Sacred Digital Dreamweaver",
  description:
    "Explore dreamweaving across Christian visions, Norse seidr, temple incubations, Tibetan dream yoga, and modern lucid practice. Find guided audio journeys, archetype meanings, and practice playbooks.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
