Fallback stills
================

Place optional default/example PNGs here (e.g., lightweight placeholders) if you want the video assembler to have images when `images/uploaded/` is empty. The assembler checks `images/uploaded/`, then session root, then this `images/example/` directory.
