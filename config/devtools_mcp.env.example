# Optional DevTools MCP config (no secrets).
# Copy to `config/devtools_mcp.env` and customize.

# Pin the chrome-devtools-mcp version for stability:
# CHROME_DEVTOOLS_MCP_VERSION=0.?.?

# Extra CLI args (optional), examples:
# CHROME_DEVTOOLS_MCP_ARGS="--headless --isolated --viewport 1280x720"
# CHROME_DEVTOOLS_MCP_ARGS="--browserUrl http://127.0.0.1:9222"
